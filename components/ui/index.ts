/**
 * Shared UI primitive barrel. Import from '@/components/ui':
 *
 *   import { PrimaryButton, TextInput, FormField } from '@/components/ui';
 *
 * Keep this file flat — only re-exports, no logic.
 */
export { PrimaryButton, SecondaryButton, DangerButton, IconButton } from './Button';
export { TextInput } from './Input';
export { FormField } from './FormField';
export { SearchInput } from './SearchInput';
